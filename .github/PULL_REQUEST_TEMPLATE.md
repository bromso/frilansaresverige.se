## What does this change?

<!-- One or two sentences. Link the issue or Slack thread if there is one. -->

## Screenshot

<!-- Required for anything visual — before/after if it's a change to an
     existing page. "Show, don't tell" is how we review. -->

## Checklist

- [ ] `bun run check:fix` leaves a clean tree
- [ ] `bun test` passes from the repo root
- [ ] `bun run build` succeeds
- [ ] New pages are registered in `apps/web/lib/routes.ts`
- [ ] New content has valid frontmatter (quoted dates!)
